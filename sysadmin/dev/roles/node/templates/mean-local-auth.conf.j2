description "Upstart script to run the mean-local-auth node.js servers"

start on runlevel [2345]
stop on runlevel [!2345]

chdir /home/deploy/mean-local-auth

respawn
respawn limit 3 30

script
    exec sudo -u www-data npm start 2>&1 >> /var/log/mean-local-auth.log
end script
